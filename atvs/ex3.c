#include <stdio.h>

int main(){
    char simbolo;

    simbolo = getchar();
    putchar(simbolo);
    simbolo = getchar();
    putchar(simbolo);
    simbolo = getchar();
    putchar(simbolo);
    simbolo = getchar();
    putchar(simbolo);

    return 0;
}